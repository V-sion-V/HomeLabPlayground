# P-001 阶段计划：平台权威数据、共享管理体验与扑克桌易用性

- 运行编号：`initial`
- 阶段编号：`P-001`
- 阶段计划修订：`1`
- 父路线图：[../../implementation-plan.md](../../implementation-plan.md)，修订 `1`
- 需求指纹：`sha256:d06b6407f4c128431551c1ff148efe8ef285b2285c291ea1b22e52265c777f4a`
- 路线图指纹：`sha256:d00cb1f130d74785a12009d8586a3815d74887e656bc6ca767d4ee917f89f434`
- 项目基线：`main@d55c2568abcb2b67871c58b53559d7b05a32232c`
- 创建日期：`2026-07-29`
- 详细程度：`expanded`
- 交付与验证策略：`relaxed`
- 验证结论：`pending`

## 1. 阶段目标、边界与关联需求

P-001 交付本功能的完整集成结果：

1. 平台层建立游戏无关的参赛事实、账户/赛季数据管理、历史匿名化、资产退役、命令幂等和恢复边界。
2. 德州扑克层修复完整结算退出、重新买入、准备和下一手选人，并完成提示音、移动座位和房间顶部易用性调整。
3. Web 平台层交付排行榜赛季选择、账户管理、赛季管理、共享控件位置和友好错误；账户、赛季、排行榜、设置和大厅不得成为扑克专属组件。
4. 在最终源码上通过全部 core/硬门禁，并使用用户授权的 iStoreOS 隔离 Docker 资源验证生产容器与 SQLite 重启；不发布或切换正式服务。

阶段覆盖 `FR-001`–`FR-013`、`AC-001`–`AC-019` 和 `NFR-001`–`NFR-011`。详细映射以父路线图第 7 节为准。

### 1.1 进入条件

- `requirements.md`、`workflow-contract.md` 和父路线图可读，需求/路线图指纹与本计划一致。
- Git 基线仍可追溯到 `d55c2568abcb2b67871c58b53559d7b05a32232c`；本功能目录之外没有会被本阶段覆盖的用户改动。
- initial 执行状态为 `ready`，P-001 是唯一 active 阶段，当前任务为“无”。
- 真实部署配置、正式 `home-table`、固定数据卷、发布目录和唯一备份不进入写入范围。

若任一条件不成立，先把执行状态设为 `paused` 并记录事实；不得开始产品修改。

### 1.2 阶段边界

- 允许修改共享契约、平台领域、Fastify 服务、Web 平台/扑克视图、相关测试、Docker smoke 测试编排、AGENTS 阶段快照和本功能工作流文件。
- 不修改扑克规则/判型/分池行为，不新增其他游戏，不新增管理员/认证，不修改 SQL schema、部署脚本、Compose、Dockerfile、固定卷或正式服务器配置。
- 如果实际实现必须触及 `packages/poker` 生产源码、`packages/persistence` 事务实现、`Dockerfile`、`.dockerignore` 或 `deploy/**`，先暂停并修订计划，说明为何现有边界不足以及新增验证。

## 2. 任务与文件范围

| 任务 | 依赖 | 结果 | 文件或范围 | 实现摘要 | 验证 | 完成条件 |
| --- | --- | --- | --- | --- | --- | --- |
| P-001-T-001 | 无 | 服务端权威、持久化兼容和删除安全在 UI 开放前成立 | `packages/contracts/src/index.ts`、`packages/domain/src/index.ts`、`apps/server/src/app.ts`、platform/server/realtime/poker 测试 | 建立平台参赛事实；修复两手间座位筹码权威；加入退役身份、匿名历史、净发行守恒、账户/赛季四类命令、租约失效、旧 JSON 归一化和安全日志 | `npm run typecheck`、`npm run test:platform`、`npm run test:poker`、`npm run test:realtime` | 成功/拒绝/no-op/重复/过期/批量回滚、退出重进/撤销/重启、排行榜资格、匿名化、资产和私牌隔离全部通过；未开放新 UI |
| P-001-T-002 | P-001-T-001 completed | 平台管理 UI、共享控件规则、扑克桌易用性和最终候选验证完成 | `apps/web/src/platform-ui.tsx`、`apps/web/src/main.tsx`、`apps/web/src/ui.tsx`、`apps/web/src/styles.css`、`apps/web/src/locales.ts`、E2E/capacity/docker smoke 测试、`AGENTS.md` 与本功能收口证据 | 交付平台排行榜/账户/赛季管理和会话清理；限制 DeviceControls；移除扑克静音生效；改善移动座位/顶部和错误文案；完成本地及隔离远端门禁 | 第 3 节最终验证流 | 全部 core/硬门禁通过；supplemental 通过或合规记录；正式服务器资源前后不变；阶段结果与 initial 收口一致 |

### 2.1 文件所有权和接口

| 文件或区域 | 任务所有权 | 目的与接口约束 |
| --- | --- | --- |
| `packages/contracts/src/index.ts` | T1 | 加法扩展 `PlatformSnapshot`、历史身份和大厅管理投影；定义平台参赛事实与删除结果。保留现有命令信封和旧字段可读性。 |
| `packages/domain/src/index.ts` | T1 | 唯一拥有账户/赛季删除、匿名化、资产退役、排行榜资格、旧状态归一化和两手间座位协调；不得把平台方法放入扑克引擎。 |
| `apps/server/src/app.ts` | T1 | Zod schema、四类平台命令、租约/房间/保护项校验、广播、自删/他删会话语义和安全日志。 |
| `tests/platform.test.ts` | T1 | 旧 JSON、参赛筛选、资产退役、匿名化、赛季清理、守恒和重启。 |
| `tests/server.test.ts` | T1 | 命令成功/失败/no-op/重复/过期/批量事务、会话失效及退出重进组合。 |
| `tests/realtime.test.ts` | T1 | 删除广播、匿名投影、公共大屏和隐藏牌隔离。 |
| `tests/poker.test.ts` | T1 验证 | 不计划改扑克规则测试；作为两手间组合变化后的规则回归门禁。 |
| `apps/web/src/platform-ui.tsx` | T2 | 新增平台专属排行榜、账户管理和赛季管理组件；不得导入牌型、底池、下注、`PokerState` 或扑克视图。 |
| `apps/web/src/main.tsx` | T2 | 接入平台组件、会话清理和页面级 DeviceControls 所有权；扑克视图只处理牌桌/手牌相关变化。 |
| `apps/web/src/ui.tsx` | T2 | 复用/补强通用 listbox、确认对话框、错误诊断样式和焦点恢复，不编码账户/赛季或扑克业务。 |
| `apps/web/src/styles.css`、`apps/web/src/locales.ts` | T2 | 双语、300px、触控、焦点、保护/危险/空状态、顶部与座位分行；不得用 CSS 隐藏代替路由层控件所有权。 |
| `tests/e2e/core.spec.ts` | T2 | Chromium/WebKit 的平台管理、自删/他删、赛季选择、错误代码、扑克退出重进、声音和显式 300px 场景。 |
| `tests/capacity.test.ts` | T2 | 15 账户、历史赛季增长、管理列表和广播有界性；不复测全部 E2E。 |
| `tests/docker-smoke.mjs` | T2 | 在既有随机资源机制内补旧状态/删除持久化与重启断言，并保留本地或远端 daemon 兼容。 |
| `AGENTS.md` | T2 | 只在阶段事实推进后更新带日期快照，不写入主机、用户、路径或凭据。 |

`packages/persistence/src/index.ts` 的现有 `PlatformStore.execute()` 事务预计无需修改；T1 通过测试证明其已满足单命令/批量原子和重放。如果证据显示事务接口本身不足，按 1.2 暂停修订，不在实现中临时扩边。

### 2.2 T1 有序执行

1. 更新执行状态为 `in_progress`、当前任务 `P-001-T-001`，记录开始基线和本任务文件范围。
2. 扩展共享契约：
   - 内部退役身份只保存公开匿名 ID、序号和退役时间，不保存原显示资料；
   - 用户可见历史以匿名标记/序号表达本地化占位；
   - 大厅只增加账户管理所需最小摘要；
   - 平台参赛事实只包含结果/游戏/赛季/参与者/有效/撤销状态。
3. 在领域构造/恢复路径补安全默认值；旧快照加载不得主动匿名化任何现存身份。
4. 建立当前扑克 `handResults` 到平台参赛事实的适配器，并让当前排行榜和新赛季归档共用一个过滤/排名函数。
5. 建立两手间座位筹码协调：
   - complete、补码、结算撤销、离开和开始下一手使用同一成员筹码边界；
   - 下一手只从当前 `Room.seats` 选择成员和筹码；
   - 已完成扑克状态保留历史/撤销，不覆盖退出后重进的座位。
6. 实现单个/批量账户删除：
   - 先校验无房间、租约、平台版本、目标和发起者保护；
   - 正余额写退役流水，净发行守恒；
   - 删除账户/资产/租约，匿名化保留历史；
   - 批量按执行时目标集合单事务提交，自删返回明确结果。
7. 实现单个/批量历史赛季删除，拒绝 current，清理关联快照/手牌/流水和仅属目标赛季的匿名辅助项。
8. 在服务层加入 schema、分发、错误码、日志和广播；测试投影不包含内部匿名映射、流水或连接租约。
9. 完成 T1 分层验证。失败时保持 T1 `in_progress`，记录实际输出和下一动作；全部通过后才写 T1 completed 检查点并进入 T2。

### 2.3 T2 有序执行

1. 更新执行状态为当前任务 `P-001-T-002`，确认 T1 证据仍对应当前源码。
2. 新增 `platform-ui.tsx`，实现：
   - 当前/历史赛季单 listbox、倒序、回退和空排行榜；
   - 账户管理的单个/批量确认、自身保护说明和目标数量；
   - 赛季管理的 current 保护、单个/批量永久删除说明；
   - 删除成功、no-op 和友好错误回调。
3. 在 App/大厅接入平台组件和会话结果：
   - 自删在任何刷新前清除最近账户和 React 状态；
   - 他删客户端由租约失效回登录；
   - 正常缓存/同步静默，错误使用友好说明与可选代码。
4. 让登录和大厅成为 `DeviceControls` 唯一渲染者；等待、玩家、观众、结算和公共大屏继续使用已存语言/主题但不显示切换。
5. 在扑克视图内移除 `party-muted` 生效与静音按钮，保留 `playTone()` 的浏览器异常保护；把剩余筹码/本轮下注改成带双语标签的独立行，并重排 300px 顶部操作。
6. 扩展 E2E、capacity 和 Docker smoke。测试使用临时数据库/随机资源，不导入用户真实数据。
7. 执行第 3 节最终门禁，完成差异和正式远端资源审计；写入阶段结果及 initial 收口证据。

## 3. 验证与完成条件

### 3.1 验证分类和阻塞规则

| 分类 | 覆盖 | 阻塞规则 |
| --- | --- | --- |
| core | FR-001–FR-013 对应行为；AC-001–AC-016；资产守恒、匿名化不可还原、current/发起者保护、私牌隔离、旧状态恢复、并发幂等、会话失效、双语和可操作性 | 任一失败、未知影响或未运行都阻塞。 |
| 项目硬门禁 | lint、typecheck、platform/server、poker、realtime、E2E 内生产 build/静态资源、capacity、隔离 Docker smoke、正式远端资源不变、`git diff --check` | 任一失败或证据不对应最终源码都阻塞。 |
| supplemental | AC-017 浏览器非关键渲染一致性、AC-018 诊断日志深度、AC-019 容量/增长体验 | `relaxed` 下只有证明不影响 core、数据、安全、隐私、构建、恢复或正式资源后，才可用 `FND-I-*` 报告；否则阻塞。 |

下一可用 finding 编号：`FND-I-001`。不得在观察失败后降低验收层级。

### 3.2 最终验证流

T1 的门禁按任务表运行。T2 完成后，在最终源码状态按以下顺序保存一次最新有效证据：

1. `npm run lint`
2. `npm run typecheck`
3. `npm run test:platform`
4. `npm run test:poker`
5. `npm run test:realtime`
6. `npm run test:e2e:core`
7. `npm run test:capacity`
8. 用户授权的隔离 `npm run test:docker-smoke`
9. `git diff --check`

`test:e2e:core` 内部的生产 build/静态资源检查作为本次 build 证据；之后的 capacity 和 Docker smoke 不修改源码，因此不重复 `npm run build`。若实现或测试过程在第 6 项后改变源码，必须从会受影响的最早门禁重跑。

不运行 `npm run test:deploy`，因为计划不修改发布接口。实际 diff 一旦触及 `deploy/**`、`.dockerignore`、Compose 或正式发布参数，先暂停修订计划并增加部署验证。

### 3.3 隔离 iStoreOS 验证

1. 复用被 Git 忽略的本机 SSH 配置或同等已批准连接边界；不得把主机、用户、私钥、发布路径或凭据写入命令输出、日志或工作流证据。
2. 连接后先只读记录正式容器的镜像/健康标识和固定卷存在性；不读取业务数据库内容。
3. Docker smoke 使用脚本生成的随机容器、镜像标签、命名卷和可访问端口。禁止使用 `home-table`、`home-party-game-platform-data`、正式发布目录或固定备份。
4. 在隔离卷验证 `linux/amd64`、离线启动、非 root、health、旧 JSON 归一化、删除/匿名化/排行状态持久化和容器重启恢复。
5. 成功后仅清理本次随机前缀资源，再次只读比较正式资源。若测试或清理失败，保留可识别诊断、把阶段设为 paused，禁止删除未知容器/卷或转而运行正式部署。

该结果只证明候选版本在真实 iStoreOS Docker 环境的隔离运行，不代表正式服务已发布。

### 3.4 阶段完成定义

P-001 只有同时满足以下条件才能完成：

1. T1、T2 均有完成检查点，实际文件全部可由本计划解释。
2. 所有 core 和项目硬门禁在最终源码状态通过。
3. 没有 unresolved 用户问题、critical/high finding、未知影响、数据/隐私/安全/兼容/构建/恢复问题或远端残留。
4. supplemental 项通过，或以连续 `FND-I-*` 记录且有独立的“无交付影响”证据。
5. 平台 UI/领域没有对牌型、底池、下注或扑克视图的新增依赖；扑克专属改动没有拥有账户、赛季、排行榜、设置或大厅。
6. 正式 `home-table`、固定卷、发布目录和唯一备份未改变；没有把隔离 smoke 描述为发布。
7. `phase-001-result.md`、`change-0.md`、`effective-requirements.md` 和 completed execution state 按合同一致写入。

## 4. 风险、恢复与修订记录

### 4.1 风险检查点

| 风险 | 执行检查点 |
| --- | --- |
| 匿名历史仍暴露原身份或合并不同玩家 | T1 在任何 UI 前检查全部历史字段、公开 ID、序号、同名重建和多删除玩家；realtime 证明映射不投影。 |
| 资产退役破坏守恒或被重复执行 | T1 覆盖单个/批量、零余额、重复 command ID、新 command ID 对已删目标、持久化失败和重启。 |
| complete 退出破坏结算撤销或下一手 | T1 覆盖退出前后撤销、重新加入、补码、准备、开手和重启；任何守恒失败阻塞 T2。 |
| 批量删除误删发起者或 current | 目标集合只在服务端执行时构造；领域 current/发起者保护独立于 UI。 |
| 自删成功后客户端先刷新而得到技术错误 | T1 返回明确 `selfDeleted`；T2 在通用刷新分支前清理会话。 |
| 平台能力进入扑克组件 | T2 新增平台文件并在差异审计中检查导入方向；违反即阻塞完成。 |
| 新数据无法安全降级 | 不在正式数据上运行候选；计划和最终记录明确旧镜像只可配合升级前完整备份。 |
| 远端隔离资源残留或误触正式服务 | 随机前缀、前后只读审计和精确清理；不确定时暂停而非扩大清理。 |

### 4.2 恢复与回滚

- 产品实现前后都以 `execution-state.md` 为协调权威；不得用 `git reset --hard`、checkout、stash 或删除用户文件恢复。
- T1/T2 失败时保留当前 diff，记录最后通过的检查、失败输出摘要和最小下一动作。只撤销本任务新改且可证明无其他所有者的局部内容。
- 本地测试使用临时 SQLite；远端测试使用随机命名卷。测试失败不修改正式数据库，不能通过删除固定卷或备份来“恢复”。
- 若候选代码已经在隔离卷执行删除，清理仅针对脚本返回并经随机前缀验证的资源。无法证明资源归属时保留现场并暂停。
- 正式部署不在本阶段。未来正式升级后若已经执行账户/赛季删除，旧版本降级必须恢复升级前完整 SQLite 备份；不能直接让旧版本解释净退役和匿名化后的状态。

### 4.3 精确恢复步骤

从当前 `ready` 状态开始或在未开始任务时恢复：

1. 读取 `execution-state.md`，确认运行编号 `initial`、阶段 `P-001`、状态 `ready` 且当前任务为“无”。
2. 运行 `git rev-parse HEAD` 与 `git status --short`，核对 `main@d55c256…` 或识别后续用户改动；不把新增改动自动归入本任务。
3. 重新计算 `requirements.md` 与 `implementation-plan.md` SHA-256，分别匹配本计划中的 `d06b6407…` 和 `d00cb1f…`。
4. 确认本计划仍是唯一 phase plan，没有 `phase-001-result.md` 或 `change-0.md`。
5. 在修改产品文件前先把 execution state 更新为 `in_progress`、当前任务 `P-001-T-001`，记录文件范围和完成条件。
6. 从 `packages/contracts/src/index.ts` 的加法契约和 `tests/platform.test.ts` 的旧状态/资产场景开始 T1。

若在任务中断，按 execution state 的最新任务范围、实际 diff 和测试证据恢复，不假定任务完成。

### 4.4 修订记录

| 修订 | 日期 | 内容 |
| --- | --- | --- |
| 1 | 2026-07-29 | 根据路线图修订 1 创建唯一 P-001 展开计划；T1 先建立平台权威与安全数据边界，T2 再开放平台 UI、完成扑克桌体验和隔离远端验证。 |
