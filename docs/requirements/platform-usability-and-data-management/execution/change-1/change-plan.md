# 平台易用性与数据管理增强：change-1 变更计划

- 运行编号：`change-1`
- 运行类型：`需求变更（缺陷修复）`
- 目标记录：`change-1.md`
- 合同版本：`3.2`
- 计划修订：`1`
- 执行模式：`single`
- 详细程度：`compact`
- 交付与验证策略：`strict`
- 验证结论：`pending`
- 当前有效需求指纹：`sha256:de95d91d6ecc362dc054d1b860909f5e7e4ffe4a7d468945c9201dd27848874c`
- 项目基线：`main@6361b5844e46acf562d58859b13ae5d11952db9f`
- 创建日期：`2026-07-29`

## 1. 变更说明审核与待生效增量

用户在正式环境实测确认两个缺陷，并于 `2026-07-29` 明确选择本运行采用
`strict` 策略。

| 变更项 | 类型 | 关联当前需求 | 变更前 | 待生效行为 | 验收影响 |
| --- | --- | --- | --- | --- | --- |
| RC-1-001 | modify | FR-001；AC-001 | `Room.seats` 已移除 complete 阶段退出者，但房间投影仍把该账户保留在 `lastResult` 的当前结算玩家列表中 | complete 阶段退出成功后，玩家、带身份观众和公共大屏的当前结算投影立即移除退出者；持久化手牌结果仍完整保留。退出者重新加入时只以新座位身份出现，不重新成为旧结算参与者 | 新增 AC-C1-001 core |
| RC-1-002 | modify | FR-004；AC-005；NFR-003 | 主题变量只由登录/大厅的切换控件注入；直接刷新牌桌或结算页时游戏界面没有控件执行初始化 | 登录恢复、直接刷新或进入等待/牌桌/结算/观战/公共展示时，应用按已保存或系统主题为正确的 main/poker scope 初始化完整语义色板；游戏内仍不显示主题入口，内容在首个可交互画面可读 | 新增 AC-C1-002 core |

### 本运行验收

| 验收 | 层级 | 可观察结果 |
| --- | --- | --- |
| AC-C1-001 | core | 在 complete 结算中离开的玩家立即从其他玩家及公共大屏的结算列表消失；底层 `handResults` 仍保留原参与者，重进不会让旧结果重新控制当前身份 |
| AC-C1-002 | core | 牌桌或结算页刷新后，`html` 具有正确 `data-theme`/`data-theme-scope` 和完整语义色板；结算面板文字、表面与操作保持可读，且游戏内没有主题切换控件 |

## 2. 当前有效状态与项目依据

- 冻结历史连续且唯一：`change-0.md` 是最后记录，指纹
  `sha256:8c3839777f94250fbc78cc9961fcdbae011459192e8d3a19937532ad624a4b47`；
  initial 状态为 `completed/passed`，没有其他 change 预留或开放 finding。
- 当前权威快照为 `effective-requirements.md`，已应用至 `change-0.md`。
- 工作区在本运行规划前干净，项目基线为上述 `main` 提交。
- `leaveRoom()` 已正确移除 `Room.seats` 并记录 `departedAccountIds`；缺陷位于
  `projectRoom()` 对 `lastResult` 的实时投影仍复制全部历史身份。
- `applyProductTheme()` 可正确注入 main/poker 色板，但只由 `ThemeToggle` 的 effect
  调用。刷新恢复到无主题控件的游戏界面时没有调用者，CSS 语义变量未初始化。

## 3. 影响分析与全局设计

1. 领域投影使用 `PokerState.departedAccountIds` 过滤 `lastResult` 中面向当前房间的
   `participantAccountIds`、派彩、玩家结果和摊牌玩家；不修改持久化
   `HandResultSummary`，不删除结算审计事实，也不改变筹码、撤销或下一手逻辑。
2. Web 主题初始化从“主题切换控件的副作用”提升为应用级生命周期：
   登录/大厅使用 `main` scope，等待室、牌桌、结算、观战和公共大屏使用 `poker`
   scope；模式继续来自 `party-theme` 或系统偏好。
3. 不修改共享命令、SQLite JSON、扑克规则、资产、部署接口或运行时依赖。
4. red-first 证据先写入领域/服务投影和真实浏览器测试，确认当前基线稳定失败后再修改
   生产代码。两个验收和全部计划硬门禁在 strict 下均阻塞。

## 4. 阶段路线图

| 阶段 | 目标 | 关联需求与验收 | 前置阶段 | 退出条件 | 当前状态 |
| --- | --- | --- | --- | --- | --- |
| P-001 | 用红灯回归锁定两个缺陷，修复结算实时投影和应用级主题初始化，并完成最终门禁 | RC-1-001–RC-1-002；AC-C1-001–AC-C1-002；FR-001、FR-004；AC-001、AC-005；NFR-003 | 无 | 红灯分别证明旧行为；目标测试、lint、typecheck、platform、realtime、Chromium/WebKit E2E、build/静态资源及差异检查全部通过；无开放 finding | ready |

P-001 是唯一阶段，包含最终集成与 change-1 收口。

## 5. 跨阶段依赖与兼容约束

- `Room.seats` 仍是当前成员权威；持久化历史结果不得因实时投影过滤而丢失或改写。
- 退出、重进、结算撤销保护、资产守恒、私牌隔离和公共大屏只读保持不变。
- 主题模式继续设备本地持久化；游戏界面只消费主题，不重新引入切换入口。
- 色板初始化不得依赖网络资源，不新增运行时依赖或修改产品主题令牌。
- 不改写 initial、`change-0.md` 或其他冻结功能历史；不触及正式服务器、部署目录、
  固定卷或备份。

## 6. 最终集成、回归与验收流程

1. red-first：
   - 增加领域投影回归，证明退出后当前 `lastResult` 仍错误包含退出者，而持久化结果
     必须保留；
   - 扩展现有真实双人牌局 E2E，在结算页刷新并验证语义色板，在玩家退出后验证其他
     玩家和公共大屏立即移除该玩家。
2. 实施领域投影过滤和应用级主题 scope 初始化。
3. 运行目标领域测试与 `npm run test:e2e:core`，确认 Chromium 与 WebKit 都通过。
4. 运行 `npm run lint`、`npm run typecheck`、`npm run test:platform`、
   `npm run test:realtime` 和 `npm run build`；未改变扑克引擎、容量或部署接口，因此
   `test:poker`、`test:capacity`、`test:deploy` 和远端 Docker smoke 不作为本变更
   必需门禁，除非实际修改或失败诊断扩大范围。
5. 运行 `git diff --check`，审计没有冻结历史、生成物或无关文件。

## 7. 需求与阶段追踪矩阵

| 需求/验收 | P-001 实现 | 验证 |
| --- | --- | --- |
| RC-1-001；FR-001；AC-001；AC-C1-001 core | 领域房间投影过滤已离开账户的当前结算身份，Web 继续消费过滤后的权威投影 | platform/server 目标断言；Chromium/WebKit 玩家与 display 结算列表 |
| RC-1-002；FR-004；AC-005；NFR-003；AC-C1-002 core | 应用级 main/poker theme scope 初始化，切换控件所有权不变 | Chromium/WebKit 结算刷新前后 data 属性、CSS 变量、计算样式及无控件断言 |
| 兼容与硬门禁 | 不改持久化历史、命令、扑克规则、隐私或依赖 | lint、typecheck、platform、realtime、E2E、build/静态资源、diff |

## 8. 风险、技术决策与修订记录

| 风险/决策 | 结论与控制 |
| --- | --- |
| 过滤实时结算误删历史 | 只在 `projectRoom()` 构造当前房间投影时过滤；测试同时断言 `state.handResults` 保留原参与者 |
| 同账户退出后重进重新绑定旧结果 | 过滤依据持久化于当前扑克状态的 `departedAccountIds`，而不是当前是否再次拥有座位 |
| 主题初始化与切换控件竞争 | 应用层只在 scope/路由变化时按同一存储值应用；主题按钮仍负责用户主动切换并写入相同键 |
| 刷新首屏闪烁或错误 scope | 初始化使用布局阶段或等价的首屏前同步路径；E2E 直接刷新结算态并检查最终可读计算样式 |
| 交付策略 | `strict`，由用户于 `2026-07-29` 明确选择；所有本运行验收和计划检查阻塞 |

| 修订 | 日期 | 原因 | 影响 |
| --- | --- | --- | --- |
| 1 | 2026-07-29 | 创建 change-1；按用户报告的两个缺陷和明确 strict 选择规划一个紧凑阶段 | RC-1-001–RC-1-002、P-001、AC-C1-001–AC-C1-002 |
