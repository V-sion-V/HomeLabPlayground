# change-1 / P-001 阶段计划

- 运行编号：`change-1`
- 阶段编号：`P-001`
- 计划修订：`2`
- 父变更计划修订：`2`
- 有效需求指纹：`sha256:13102c269ca01101b3e21671663cb1619ecd33d5d3c746a8451120721dda2f16`
- 变更计划指纹：`sha256:4171f79514a18806cbcc35caf36805fec201be493da9d117ce8a692d273707ed`
- 项目基线：Git `6bc0daaa726ebb5ee6a142e36202d2ae583f1383`
- 创建日期：`2026-07-27`
- 详细程度：`compact`
- 交付与验证策略：`relaxed`
- 验证结论：`pending`

## 1. 阶段目标、边界与关联需求

在一个可独立恢复的阶段内完成 RC-1-001–RC-1-005：修复窄屏牌桌、调整玩家顶栏、增加全局花色预设、平滑倒计时，并把自动下一手改为持久化结算面板和全员准备门槛。

本阶段不修改扑克下注、底池分配、赛季、排行榜、账户认证边界、Docker 拓扑或资产换算规则。所有新验收 AC-C1-001–AC-C1-005 均为 `core`。

## 2. 任务与文件范围

| 任务 | 结果 | 文件或范围 | 实现摘要 | 验证 | 完成条件 |
| --- | --- | --- | --- | --- | --- |
| P-001-T-001 | 权威契约、持久化摘要和准备状态机完成 | `packages/contracts/src/index.ts`、`packages/domain/src/index.ts`、`packages/poker/src/index.ts`、`apps/server/src/app.ts`、平台/扑克/实时测试 | 增加花色预设、扩展结算摘要、牌型标签、准备集合与命令；移除 complete 自动推进；保护摊牌公开边界及旧快照归一化 | `npm run typecheck`、`npm run test:platform`、`npm run test:poker`、`npm run test:realtime` | 净变化、实际摊牌公开、弃牌保密、断线/零筹码阻塞、全员准备原子开局及旧数据恢复都有确定性通过证据 |
| P-001-T-002 | 响应式玩家端/大屏与最终集成完成 | `apps/web/src/main.tsx`、`apps/web/src/styles.css`、`apps/web/src/locales.ts`、`tests/e2e/core.spec.ts` 及受影响测试 | 重排顶栏；统一彩色牌面；以 transform 动画替代宽度阶梯；增加玩家/大屏结算浮层和准备/补码入口；消除窄屏页面溢出 | 本地运行 `npm run lint`、`npm run typecheck`、`npm run test:e2e:core`、`npm run build`，并复验可能被 UI 契约影响的定向测试；不运行 Docker | 窄屏、双语、主题、倒计时、结算及准备端到端通过，构建成功且全部本地 core 验收闭合；如仍需部署验收则暂停交接 |

任务依赖：P-001-T-002 依赖 P-001-T-001 的投影、设置与命令契约。

## 3. 验证与完成条件

| 门禁 | 层级 | 可观察结果 |
| --- | --- | --- |
| AC-C1-001 | core | 320px 及更窄视口无页面级横向滚动，顶栏、结算和下注主要控件可触达 |
| AC-C1-002 | core | 房间名居中，当前玩家名在其下；语言与静音同组位于最右并在手机上可用 |
| AC-C1-003 | core | 两种花色预设可在全局设置保存、恢复，并一致作用于玩家端和大屏 |
| AC-C1-004 | core | 倒计时填充使用连续 transform 动画，文本局部更新；暂停/恢复和服务端截止仍正确 |
| AC-C1-005 | core | 结算浮层保持至全员准备；显示所有参与者签名净变化；真实摊牌仅公开未弃牌者并标记赢家牌型；断线/零筹码阻塞；最后准备原子开下一手 |
| G-C1-001 | hard gate | lint、typecheck、受影响单元/服务/实时/E2E 及生产构建全部通过 |

阶段完成时不得存在未解决的隐私、资产、并发、持久化兼容、构建或 core 验收问题。`relaxed` 模式下，只有已独立证明不影响交付行为的 supplemental 异常可作为 `FND-C1-*` 保留。

## 4. 风险、恢复与修订记录

- 生产文件编辑前先把执行状态和本阶段标记为 `in_progress`，记录当前任务及范围。
- 任务一中断时从契约、快照归一化、命令 schema、结果投影和测试差异恢复；不得用旧测试通过推断新隐私边界完成。
- 任务二中断时保留任务一完成检查点，依据 UI 文件差异和最后一次浏览器/构建结果恢复。
- 若发现公开了弃牌或进行中手牌、准备造成重复发牌/盲注、或旧快照无法加载，立即将阶段设为 `blocked`，不得以 relaxed finding 结项。
- 修订 1（2026-07-27）：创建首个且唯一可执行阶段计划。
- 修订 2（2026-07-27）：按用户要求改为本地优先且禁止本运行尝试 Docker；必要部署验证必须暂停交接。
