# change-2 / P-001 阶段结果：结算总筹码与可操作补码弹窗

- 运行编号：`change-2`
- 阶段编号：`P-001`
- 阶段计划：[phase-001-plan.md](phase-001-plan.md)
- 阶段计划修订：`1`
- 父变更计划修订：`1`
- 开始基线：Git `f671f71c24a9f12473e58da13c01cc9e2002d8b7`
- 完成源状态：以上述基线为起点的当前工作区实现与本记录所列文件差异
- 完成日期：`2026-07-27`
- 状态：`completed`
- 交付与验证策略：`relaxed`
- 验证结论：`passed`

## 1. 阶段目标与结果

P-001 完成了 RC-2-001–RC-2-002。新结算结果为每名参与者保存结算后的 `endingChips`，玩家端和公共大屏的结算玩家行现在同时显示本手签名筹码变化与“总筹码”。玩家仍占座时总量优先使用实时座位筹码，因此补码后立即更新；玩家已离座时使用结算快照，旧结果缺少快照时显示占位而不伪造数值。

补码 modal 已从 `.action-dock` 的较低堆叠上下文移到牌桌根级兄弟节点。结算面板存在时，玩家可从面板打开补码 modal、输入合法数值、确认提交并看到总筹码更新；补码限额、资产流水和 complete 阶段授权逻辑未改变。

## 2. 任务、需求与验收覆盖

| 任务 | 完成结果 | 需求范围 | 主要证据 |
| --- | --- | --- | --- |
| P-001-T-001 | 完成 | RC-2-001–RC-2-002 | 类型检查、platform/server 15/15、生产 Chromium/WebKit 4/4、生产构建、静态资源和差异检查 |

| 验收 | 层级 | 通过证据 |
| --- | --- | --- |
| AC-C2-001 | core | 服务测试断言三人结算分别保存 2,200、1,900、1,900，并验证两人结算总量守恒；E2E 断言玩家端和大屏每个结算行均显示“总筹码”，补码后实时增加 |
| AC-C2-002 | core | Chromium 桌面和 WebKit 手机均在结算面板存在时打开补码 modal、填写 100、点击确认并观察 modal 关闭和筹码更新 |
| G-C2-001 | hard gate | typecheck、定向服务测试、生产浏览器流程、Vite/tsup 构建、静态资源与 `git diff --check` 全部通过 |

## 3. 文件修改

| 文件 | 修改模式 | 主要目的 |
| --- | --- | --- |
| `packages/contracts/src/index.ts` | modify | 为结算玩家摘要增加兼容性 `endingChips` |
| `packages/domain/src/index.ts` | modify | 保存新结算结果的结算后筹码快照 |
| `apps/server/src/app.ts` | modify | 把每名玩家结算完成后的 stack 写入结果摘要 |
| `apps/web/src/main.tsx` | modify | 显示实时/快照总筹码并把补码 modal 移出操作区堆叠上下文 |
| `apps/web/src/styles.css` | modify | 增加结算筹码摘要列样式 |
| `apps/web/src/locales.ts` | modify | 增加“总筹码 / Total chips”本地化文本 |
| `tests/server.test.ts` | modify | 验证总筹码持久化、投影和守恒 |
| `tests/e2e/core.spec.ts` | modify | 验证玩家端/大屏总量与实际补码交互 |

## 4. 测试与验证

| 验证 | 观察结果 |
| --- | --- |
| `npm run typecheck` | 通过 |
| `npm run test:platform` | 通过：platform/server 2 个文件、15/15 |
| `npm run test:e2e:core` | 通过：生产构建和静态资源检查成功；Chromium 桌面与 WebKit 手机 4/4 |
| 结算补码交互 | 两种浏览器均在结算层存在时成功输入 `100`、确认补码并观察总筹码增加 |
| `git diff --check` | 通过：无空白错误 |
| Docker/部署验证 | 按用户既有明确要求未运行；本阶段不修改部署配置 |

## 5. 发现项与处置

无开放 `FND-C2-*` 报告项。下一个可分配编号为 `FND-C2-001`。

| ID | 类别 | 严重程度 | 关联需求/验收 | 观察与证据 | 最终功能影响 | 处置 | 置信度 | 建议后续 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| — | — | — | — | 无开放发现项 | 无 | — | 高 | — |

## 6. 决策、计划偏差与恢复记录

- 保持用户明确选择的 `relaxed` 策略；两项 core 与硬门禁实际通过，未使用 `passed_with_findings`。
- `endingChips` 在共享结果中保持可选以兼容 change-1 旧快照；新结算结果始终写入该字段。
- UI 优先使用当前座位的实时 `tableChips`，使结算阶段补码立即反映；离座后回退到结果快照，避免丢失结算总量。
- 层级修复通过移动 modal 挂载位置完成，没有提高整个操作区层级或改变结算面板的拦截规则。
- 实现、范围和验证均与计划一致，无恢复事件或计划偏差。

## 7. 遗留风险与下一阶段进入条件

没有阻止交付的遗留风险、未决产品问题或开放 finding。P-001 是 change-2 唯一阶段，不存在后续 change-2 阶段。

部署操作不属于本次授权范围；后续产品需求应从更新后的 `effective-requirements.md` 建立新的 `change-N`。
